plugins {
	alias(libs.plugins.android.library)
}

android {
	namespace "com.khopan.core"
	compileSdk {
		version = release(35)
	}

	defaultConfig {
		minSdk 26
		targetSdk 35
	}

	buildTypes {
		release {
			minifyEnabled false
			proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
		}
	}

	compileOptions {
		sourceCompatibility JavaVersion.VERSION_11
		targetCompatibility JavaVersion.VERSION_11
	}
}

dependencies {
	implementation "io.github.tribalfs:oneui-design:0.8.13+oneui8"
}
